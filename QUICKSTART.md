# ⚡ QUICK START - PBK CRM

## 🎯 Что это?

Единая CRM система PBK Construction с:
- ✅ Управлением лидами/клиентами
- ✅ Звонками через Retell AI (с workflow подтверждения)
- ✅ Корпоративной почтой
- ✅ AI генерацией предложений (OpenAI)
- ✅ Telegram ботами
- ✅ Copilot AI-агентом для автоматизации

**Домен:** https://crm.pbkconstruction.net

---

## 📁 Где что находится?

```
/root/pbk-crm-unified/
├── README.md              ← Полное описание
├── SETUP.md               ← Инструкция по установке
├── CREDENTIALS_GUIDE.md   ← Все креды и как их получить
├── DEVELOPMENT_ROADMAP.md ← План разработки
├── .env.example           ← Пример переменных окружения
│
├── frontend/              ← Next.js приложение (порт 3000)
├── backend/               ← Express API (порт 5000)
├── copilot-agent/         ← Telegram AI бот
├── database/              ← PostgreSQL схемы
└── docker-compose.yml     ← Запуск всего через Docker
```

---

## 🚀 Как запустить?

### Вариант 1: Docker (рекомендуется)

```bash
cd /root/pbk-crm-unified

# 1. Настроить .env
cp .env.example .env
nano .env  # заполнить креды

# 2. Запустить всё
docker-compose up -d

# 3. Создать схему БД
docker-compose exec backend npm run db:migrate

# 4. Открыть
# https://crm.pbkconstruction.net
```

### Вариант 2: Development

```bash
cd /root/pbk-crm-unified

# 1. Установить зависимости
npm install

# 2. Настроить .env
cp .env.example .env
nano .env

# 3. Запустить PostgreSQL отдельно
docker-compose up -d postgres redis

# 4. Создать схему
npm run db:migrate

# 5. Запустить dev режим
npm run dev
# Frontend: http://localhost:3000
# Backend: http://localhost:5000

# 6. (опционально) Copilot Agent
npm run dev:copilot
```

---

## 🔑 Обязательные креды

Минимум для запуска (в `.env`):

```bash
# База данных
DATABASE_URL=postgresql://pbk_admin:password@localhost:5432/pbk_crm

# JWT
JWT_SECRET=<random_string_64_chars>

# Retell AI (для звонков)
RETELL_API_KEY=key_xxxxx
RETELL_AGENT_ID=agent_xxxxx

# OpenAI (для предложений)
OPENAI_API_KEY=sk-xxxxx

# Telegram (основной бот)
TELEGRAM_BOT_TOKEN=xxxxx:xxxxx

# Telegram (Copilot бот)
COPILOT_TELEGRAM_BOT_TOKEN=xxxxx:xxxxx

# Корпоративная почта
SMTP_USER=info@pbkconstruction.net
SMTP_PASSWORD=xxxxx
```

Полный список: см. `CREDENTIALS_GUIDE.md`

---

## 📋 После установки

1. **Откройте:** https://crm.pbkconstruction.net
2. **Войдите:** admin@pbkconstruction.net / admin123
3. **Смените пароль!**
4. **Настройте сервисы:** Settings → все вкладки

---

## 🎯 Основные возможности

### 1. Звонки через Retell AI

**Создание звонка:**
1. Карточка клиента → "Создать звонок"
2. Заполнить форму (номер, цель, инструкции)
3. Отправить на подтверждение

**Подтверждение:**
1. Пользователь с правами получает уведомление
2. Проверяет → подтверждает/отклоняет
3. После подтверждения → звонок автоматически

**После звонка:**
- Расшифровка автоматически
- Можно перевести на любой язык
- Отображается в истории клиента

### 2. Copilot Agent (Telegram)

Найти бота → `/start`

**Команды:**
```
Создай лид для компании ABC
Покажи звонки за сегодня
Отправь письмо клиенту
Создай предложение для лида #123
Статистика за неделю
```

### 3. Воронки продаж

Settings → Pipelines → "Создать воронку"

Можно описать своими словами, AI создаст автоматически.

### 4. Коммерческие предложения

Лид → "Создать предложение" → AI сгенерирует по шаблону

---

## 📊 Структура системы

```
Frontend (Next.js)
    ↓
Backend API (Express)
    ↓
┌───────────┬──────────┬──────────┐
PostgreSQL  Redis      External APIs
                       ├─ Retell AI
                       ├─ OpenAI
                       ├─ SMTP/IMAP
                       └─ Telegram

Copilot Agent (отдельно)
    ↓
Telegram Bot
```

---

## 🛠️ Полезные команды

```bash
# Запустить всё
docker-compose up -d

# Остановить
docker-compose down

# Логи
docker-compose logs -f backend
docker-compose logs -f frontend

# Перезапустить сервис
docker-compose restart backend

# Выполнить миграцию
docker-compose exec backend npm run db:migrate

# Консоль PostgreSQL
docker-compose exec postgres psql -U pbk_admin -d pbk_crm

# Бэкап БД
docker-compose exec postgres pg_dump -U pbk_admin pbk_crm > backup.sql
```

---

## 🐛 Troubleshooting

**Проблема:** Не запускается frontend
```bash
# Проверить логи
docker-compose logs frontend

# Пересобрать
docker-compose build frontend
docker-compose up -d frontend
```

**Проблема:** Ошибка подключения к БД
```bash
# Проверить что PostgreSQL запущен
docker-compose ps postgres

# Проверить .env
cat .env | grep DATABASE_URL
```

**Проблема:** Retell звонки не работают
- Проверить RETELL_API_KEY в Settings
- Проверить webhook настроен в Retell
- Проверить логи: `docker-compose logs backend`

**Проблема:** Telegram бот не отвечает
- Проверить токен в .env
- Проверить бот запущен: `docker-compose ps copilot-agent`
- Отправить боту /start

---

## 📚 Дальнейшее чтение

- **README.md** - Полное описание всех возможностей
- **SETUP.md** - Детальная установка с нуля
- **CREDENTIALS_GUIDE.md** - Где и как получить все креды
- **DEVELOPMENT_ROADMAP.md** - План разработки и задачи

---

## 🎯 Статус проекта

**Готово:**
✅ Структура проекта
✅ База данных
✅ Базовый backend API
✅ Copilot Agent
✅ Документация

**В разработке:**
🚧 Frontend интерфейс
🚧 Интеграции (Retell, OpenAI, Email)
🚧 Telegram боты
🚧 Real-time обновления

**Прогресс:** ~25%

---

## 📞 Поддержка

При проблемах:
1. Проверить логи
2. Проверить .env
3. Прочитать документацию
4. Проверить DEVELOPMENT_ROADMAP.md

---

**Версия:** 1.0.0  
**Создано:** Декабрь 2024  
**Для:** PBK Construction

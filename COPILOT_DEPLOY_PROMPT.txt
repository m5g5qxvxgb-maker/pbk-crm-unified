# 🚀 ПРОМПТ ДЛЯ COPILOT - РАЗВЕРТЫВАНИЕ PBK CRM НА СЕРВЕРЕ

Скопируй этот промпт и отправь Copilot на сервере:

---

Мне нужно развернуть CRM систему на этом сервере. Вот что нужно сделать:

## Задача:
Установить и запустить production-ready CRM систему PBK CRM Unified с GitHub.

## Репозиторий:
https://github.com/m5g5qxvxgb-maker/pbk-crm-unified

## Шаги:

### 1. Проверь систему и установи зависимости:
- OS: Ubuntu/Debian
- Установи: Docker, Docker Compose, Git (если нет)
- Проверь свободные порты: 3000, 5001, 5432

### 2. Клонируй репозиторий:
```bash
cd /opt
git clone https://github.com/m5g5qxvxgb-maker/pbk-crm-unified.git
cd pbk-crm-unified
```

### 3. Настрой .env файл:
- Скопируй .env.example в .env
- Замени следующие значения:
  - `POSTGRES_PASSWORD` - сгенерируй сложный пароль (32 символа)
  - `JWT_SECRET` - сгенерируй случайную строку (64 символа)  
  - `NEXT_PUBLIC_API_URL` - укажи: http://IP_ЭТОГО_СЕРВЕРА:5001

Используй для генерации: `openssl rand -base64 32`

### 4. Запусти через Docker:
```bash
./deploy.sh production
```

### 5. Проверь что всё работает:
- Backend health: `curl http://localhost:5001/health`
- Frontend: `curl -I http://localhost:3000`
- Docker контейнеры: `docker-compose ps`

### 6. Опционально - настрой Nginx и SSL:
- Установи Nginx
- Настрой reverse proxy для портов 3000 и 5001
- Установи SSL через Let's Encrypt (если есть домен)

## Важные детали:

**Учетные данные по умолчанию:**
- Email: admin@pbkconstruction.net
- Password: admin123
(Измени после первого входа!)

**Порты:**
- 3000 - Frontend (Next.js)
- 5001 - Backend API (Express)
- 5432 - PostgreSQL (внутри Docker)

**API ключи:**
- Все необходимые ключи уже настроены в .env.example
- Telegram Bot, Retell AI, OpenAI - готовы к использованию
- Просто скопируй .env.example в .env

**Troubleshooting:**
Если что-то не работает:
- Логи Docker: `docker-compose logs -f`
- Логи отдельного сервиса: `docker-compose logs backend`
- Перезапуск: `./deploy.sh restart production`
- Статус: `./deploy.sh status`

## Ожидаемый результат:
После выполнения всех шагов:
- CRM доступна по адресу: http://SERVER_IP:3000
- API работает: http://SERVER_IP:5001
- Все 4 Docker контейнера запущены (postgres, backend, frontend, telegram-bot)
- Health check возвращает: {"status":"ok"}

## Дополнительно:
Полная документация в репозитории:
- README.md - обзор системы
- DEPLOYMENT_GUIDE.md - детальный гайд
- QUICK_DEPLOY.md - быстрые команды

---

Выполни все шаги последовательно. После каждого шага сообщи результат. Если возникнут ошибки - покажи их и предложи решение.

Начинай с шага 1. Готов?

const https = require('https');

const BOT_TOKEN = '8003573668:AAHHs6GUJx-pUEL-fxe_lwVJCRutftrCZ30';
const CHAT_ID = '533868685';

const message = `
🎨 НОВЫЙ ДИЗАЙН СТРАНИЦЫ ЛОГИНА

✅ Страница логина полностью переделана!

📋 Что изменилось:
━━━━━━━━━━━━━━━━━━━━━━
• ✨ Современный темный дизайн
• 🌈 Анимированный градиентный фон
• 💎 Glassmorphism эффект (стекло)
• 🎯 Иконки для полей ввода
• ⚡ Плавные анимации и переходы
• 🚀 Кнопка "Заполнить тестовыми данными"
• 📱 Полностью адаптивный дизайн
• ✅ Индикатор "Система работает"

🎨 Стилистика:
━━━━━━━━━━━━━━━━━━━━━━
• Темный градиент (slate-900 → slate-800)
• Оранжевые акценты (фирменный цвет)
• Размытые цветные блобы на фоне
• Современная типографика
• Rounded corners 3xl

🔐 Доступы:
━━━━━━━━━━━━━━━━━━━━━━
Email: admin@pbkconstruction.net
Пароль: admin123

🌐 Ссылки для доступа:
━━━━━━━━━━━━━━━━━━━━━━
1. Основной домен:
   https://crmpbk.appp2p.uk

2. Tailscale VPN:
   http://100.91.124.46:3333

3. Локальная сеть:
   http://192.168.1.96:3333

⚠️ ВАЖНО: Если основной домен показывает ошибку Cloudflare 403:
━━━━━━━━━━━━━━━━━━━━━━
Нужно зайти в панель Cloudflare и:
1. Отключить "Under Attack Mode"
2. Поставить Security Level: Medium
3. Очистить кэш

💡 До исправления Cloudflare используйте Tailscale или локальный доступ.

🎉 Система готова к использованию!
`;

function sendTelegramMessage(text) {
  const url = `https://api.telegram.org/bot${BOT_TOKEN}/sendMessage`;
  const data = JSON.stringify({
    chat_id: CHAT_ID,
    text: text
  });

  const options = {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Content-Length': data.length
    }
  };

  const req = https.request(url, options, (res) => {
    let body = '';
    res.on('data', (chunk) => body += chunk);
    res.on('end', () => {
      console.log('✅ Сообщение отправлено в Telegram');
      console.log('Response:', body);
    });
  });

  req.on('error', (error) => {
    console.error('❌ Ошибка отправки:', error.message);
  });

  req.write(data);
  req.end();
}

sendTelegramMessage(message);
